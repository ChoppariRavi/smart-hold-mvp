{
    "name": "smart-hold-root",
    "scripts": {
      "dev:frontend": "cd frontend && npm run dev",
      "dev:backend": "cd backend && uv run uvicorn src.main:app --reload",
      "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
    },
    "devDependencies": {
      "concurrently": "^8.2.2"
    }
  }